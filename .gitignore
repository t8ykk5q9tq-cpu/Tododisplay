# Python virtual environment
venv/
__pycache__/
*.pyc

# Local database (don't sync your list data)
lists.db

# OS / editor cruft
.DS_Store
*.swp

# Runtime log from update.sh
update.log

# Time tracker: secrets and runtime data (never commit — repo is public)
tracker_config.py
tracker_log.json
tracker_state.json
tracker.log
