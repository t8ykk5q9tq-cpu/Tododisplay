# Python virtual environment
venv/
__pycache__/
*.pyc

# Local database (don't sync your list data)
lists.db

# OS / editor cruft
.DS_Store
*.swp

# Runtime logs
update.log
watchdog.log
run.log

# Time tracker: secrets and runtime data (never commit — repo is public)
tracker_config.py
tracker_log.json
tracker_state.json
tracker.log
app_usage.json
mood_log.json
sleep_log.json
water_log.json
metric_log.json
journal_log.json
usage_minutes.json
mac_usage_minutes.json
compliance.json
pushover_count.json

# Nightly backups of runtime data
backups/
